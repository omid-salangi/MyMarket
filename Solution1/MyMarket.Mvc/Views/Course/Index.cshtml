@model Application.ViewModel.CourseViewModel
@{
    ViewData["Title"] = "Index";
}

<div class="container">
    <div class="row">
        @foreach (var course in Model.Courses)
        {
            <div> <img src="@course.ImageUrl"/></div>
        }
    </div>

</div>

